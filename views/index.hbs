<div class="jumbotron">

  <div class="col-sm-10 mx-auto">
    <h1 class="display-4 mb-4">Используемые технологии</h1>
    <div class="mx-auto row">
      <div class="col-lg-5">
        <h4>Node.js</h4>
        <p class="info-h">Express, Handlebars, Passport, Mongoose, Socket.io, PM2, Google&VK OAuth</p>

        <h4>Ubuntu Server</h4>
        <p class="info-h">Firewall, Public Key Authentication</p>
      </div>

      <div class="col-lg-5">
        <h4>Nginx</h4>
        <p class="info-h">SSL Encryption A+ Grade, Reverse Proxy, IPv6</p>

        <h4>MongoDB</h4>
        <p class="info-h">Session Store, Users</p>
      </div>
    </div>

    <h1 class="display-4 mt-5">GitHub</h1>

    <a class="github-button" href="https://github.com/bondarevk" data-size="large" aria-label="Follow @bondarevk on GitHub">Follow @bondarevk</a>
    <a class="github-button" href="https://github.com/bondarevk/server" data-icon="octicon-eye" data-size="large" aria-label="Watch bondarevk/server on GitHub">Watch</a><!-- Place this tag where you want the button to render. -->
    <a class="github-button" href="https://github.com/bondarevk/server" data-icon="octicon-star" data-size="large" aria-label="Star bondarevk/server on GitHub">Star</a>


    <h1 class="display-4 mt-5">Пользователи</h1>
    <div class="table-responsive">
      <table class="table table-striped table-responsive">
        <thead>
        <tr>
          <th>#</th>
          <th>Ник</th>
          <th>Вконтакте</th>
          <th>Google</th>
          <th>Дата регистрации</th>
        </tr>
        </thead>
        <tbody>
        {{#each users}}
          <tr>
            <th scope="row">{{inc @index}}</th>
            <td>{{this.username}}</td>

            {{#if this.vkontakte.id}}
              <td>{{this.vkontakte.profileUrl}}</td>
            {{else}}
              <td>-</td>
            {{/if}}

            {{#if this.google.id}}
              <td>{{firstofEmails this.google.emails}}</td>
            {{else}}
              <td>-</td>
            {{/if}}

            <td>{{formatDate this.createdAt}}</td>
          </tr>
        {{/each}}
        </tbody>
      </table>
    </div>

  </div>
</div>
</div>